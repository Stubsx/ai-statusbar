# 隐私说明

灵眸的核心功能在本机完成。项目不包含开发者控制的遥测、广告、分析服务或数据收集服务器。

## 本地读取的数据

为了判断状态和计算用量，灵眸可能读取已安装工具的以下本地数据：

- 进程列表和已建立连接的本地端口；
- Codex、Kimi Code、Kimi Work、Claude Code、Hermes、ZCode、DSH 的会话数据库、日志或索引；
- 会话 ID、任务标题、更新时间、事件状态和 Token 计数；
- 联网配额功能所需的本地登录状态或访问令牌；此功能默认开启，可随时在设置中关闭。

会话正文仅在本机解析状态和计数，不会发送给灵眸项目维护者。任务标题会显示在本机界面；启用通知后，也可能显示在通知中心或锁屏。

## 网络请求

联网配额默认开启，查询时可能把对应厂商的访问令牌发送回该厂商的 HTTPS 接口；用户可在设置中关闭：

- `chatgpt.com`：Codex/ChatGPT 配额；
- `api.kimi.com`、`www.kimi.com`：Kimi Code 或 Kimi 会员配额；
- `open.bigmodel.cn` 或 `api.z.ai`：ZCode Coding Plan 配额。

灵眸不会把一个厂商的令牌发送给另一个厂商，也不会把令牌写入自身日志或配额缓存。这些接口可能由厂商调整或停用。
Kimi Code 的 7天/5小时配额来自 `api.kimi.com`；检测到 Kimi Work 时，其本月会员额度来自 `www.kimi.com`。两者独立查询；未检测到 Kimi Work 时不会请求或展示月度额度。

Kimi 新版加密凭证的解密默认关闭。开启后，后台读取钥匙串 `kimi-desktop Safe Storage` 始终禁止授权界面：无权限或钥匙串锁定时直接降级，已有额度保留并标注提示。只有设置中的「读取 Kimi 月度额度 → 连接并开启」会启动一次独立的交互请求，同一时间最多一个，60 秒超时，不会自动重试；拒绝、取消或超时后解密保持关闭。`lingmou-collector --authorize-kimi-keychain` 是相同的手动入口，不得用于定时脚本，也不会输出口令或令牌。钥匙串的「始终允许」只认当次构建的程序哈希，重建或替换灵眸后系统授权必然失效，因此授权成功时会把该口令备份到本机 `~/.ai-statusbar/kimi-safe-storage.key`（目录 0700、文件 0600，仅当前用户可读），后台读取在钥匙串拒绝时回退到该备份，不再反复要求授权。口令只用于本机解密，不会发送或写入日志；关闭「读取 Kimi 月度额度」会立即删除该备份文件，也可随时手动删除。Kimi 桌面端轮换口令后备份失效，需重新连接一次。

手动点击「检查更新」时向 `api.github.com` 查询本项目的正式发布信息，不发送会话、配额令牌或设备标识；点击下载入口由浏览器访问 GitHub 项目发布页。没有默认后台更新检查或自动安装。

点击 Kimi Code 任务时，灵眸读取本机 Web 服务登记及进程身份，并核验监听端口。按需向已验证的本机回环地址请求健康状态、指定会话元数据和连接订阅；认证使用 `.kimi-code/server.token` 中已有的服务令牌，仅放入本机请求的 Authorization 头，拒绝 HTTP 重定向、不使用系统代理，不写入缓存、日志或浏览器链接。该查询用于会话跳转，与联网配额开关独立，不在定时采集中发起。默认浏览器自行使用已有的 Kimi 登录状态；未连接时由 Kimi 页面要求认证。灵眸不改写服务令牌、不自动启动服务或发送模型提示词。

## 用量同步

用量同步默认关闭。开启后，灵眸会把“日期、工具名、输入/输出/缓存 Token 计数”导出为同步目录中的 `usage-<设备ID>.json`，并读取同一目录下其他设备导出的同名格式文件用于汇总。默认目录是 iCloud Drive 的 `灵眸` 文件夹，也可改为其他网盘或同步工具的目录。

- 导出内容不含任务标题、会话内容、路径或账号信息；文件保留最近 365 天。
- 每台设备只写自己的文件、只读其他设备的文件，不经过灵眸项目维护者的任何服务器。
- 目录数据的实际传输由所选网盘/同步服务完成，其隐私政策适用于传输与存储过程；请勿与他人共用同一个同步目录。
- 账号配额是账号级数据，不参与同步合并；关闭同步后删除目录中的 `usage-*.json` 即可移除已导出的计数。

## 本地写入的数据

灵眸在 `~/.ai-statusbar/` 保存设置、聚合后的 Token 用量、配额缓存和连接状态。它们不会被上传。仅在开发者显式设置 `LINGMOU_DEBUG=1` 时，背景采样才会追加诊断日志。卸载应用后可手动删除该目录。

为了减少重复解析，`source-state-v1.json` 在本机缓存近期会话的文件路径、文件属性和已解析的状态信号（不缓存正文）；`collector-metrics.json` 保存统计与配额。文件变化监听和数据库属性检查均在本机完成，不需要屏幕录制权限。

灵眸不会刷新或改写各工具自己的凭据文件。令牌过期时，配额会暂时隐藏，等待对应工具自行完成登录或刷新。

## 最近事件与脚本接口

`task-history.json` 在本机保存最近 7 天、最多 200 条任务事件，包括工具、会话 ID、标题、状态、时间及已查看标记；去重标识保留最多 30 天 / 5000 条。它不参加用量同步。可在「记录」页清空，仍在等待用户的任务和去重状态继续保留，避免重复提醒。`quota-alerts.json` 仅保存配额窗口和提醒去重状态，最长 35 天。

本地事件接口默认关闭。开启后，`events-v1.json` 保存最多 7 天 / 500 条新事件，供本机脚本读取；标题默认不包含，需额外开启。演示模式会移除接口中保留的标题。关闭接口会清空其保留事件并停止导出。历史和接口文件权限为 0600，目录为 0700；同一系统账号下的其他程序仍可能读取。接口没有网络服务，也不会自动运行脚本；用户自行配置的外部自动化遵循它自己的数据去向。已被外部程序复制的内容不能通过关闭接口撤回。

桌宠 ZIP 导出包含所选素材、作者署名、作品元数据和预览，不包含会话、设置或账号信息。导出是本地文件操作，不自动上传。作品目录随 App 打包，没有远程市场请求。

## 屏幕录制

“背景自适应”外观会截取桌面卡片下方区域，缩小到最长边约 24 像素后只计算平均亮度。图像只存在于内存中，不写入磁盘、不通过网络发送。该功能需要 macOS 屏幕录制权限，并且只有用户选择该模式后才应申请。

## 通知

明确结束、等待回答、中断与停止活动通知可能包含任务标题。演示模式隐藏面板、菜单、桌宠提示与新通知的任务标题，并清除灵眸已经投递的通知；暂时静音会停止新提醒。配额阈值与恢复提醒默认关闭。共享电脑或锁屏隐私要求较高时，请保持通知关闭，或在 macOS 系统设置中关闭通知预览。

## 删除数据

退出灵眸并删除 `~/.ai-statusbar/` 即可删除本项目产生的本地设置与缓存。灵眸不会删除各 AI 工具自己的会话或账号数据。
